{
  "buildCommand": "cd frontend && npm ci && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "cd frontend && npm ci",
  "rewrites": [
    { "source": "/api/kaiten/:path*", "destination": "/api/kaiten" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
